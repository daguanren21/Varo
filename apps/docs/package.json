{
  "name": "@varo/docs",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "vitepress build .",
    "clean": "rimraf .vitepress/dist dist",
    "dev": "vitepress dev .",
    "lint": "echo docs lint pending",
    "preview": "vitepress preview .",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:e2e": "echo docs e2e skipped"
  },
  "dependencies": {
    "@varo/theme": "workspace:*",
    "@varo/ui-h5": "workspace:*",
    "vitepress": "^1.6.4",
    "vue": "^3.5.39"
  }
}
