# Contributing

## Recommended implementation order

1. Add the interaction model to `primitives-core`
2. Adapt it for `primitives-h5` and `primitives-weapp`
3. Build official wrappers in `ui-h5` and `ui-weapp`
4. Finish with tests and docs