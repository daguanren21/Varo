# Mini-program Example

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { VButton, VInput } from '@varo/ui-weapp'

const mobile = ref('')
</script>
```

- mini-program integration is currently pinned to `wevu@6.10.2`
- start from `@varo/primitives-weapp` if you need an internal wrapper layer